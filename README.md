# Simple React.js app

Build used vite.js, react, tailwindcss

## Local setup

Clone, install npm modules, run.

Now little more details:

```
git clone ...
cd app
npm install
npm run dev
```

Open localhost.

